# paper_outputs/

Paper-ready plots and tables land here. This directory is git-ignored; regenerate with `analysis/make_paper_outputs.py`, `analysis/compare_modes.py`, or the cluster runner, then bundle with `analysis/package_paper_outputs.py` for sharing.
