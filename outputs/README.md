# outputs/

Run outputs live here (ROOT merges, logs, WRL dumps). The folder is git-ignored; re-run the pipelines (e.g., `analysis/cluster_run_cad_vs_none_prod.py` or `scripts/run_parallel_batches.py`) to regenerate. Keep only what you need locally and trim old batches to save space.
